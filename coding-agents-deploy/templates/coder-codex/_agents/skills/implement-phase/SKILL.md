---
name: implement-phase
description: Implement one {{project_name}} phase from {{plan_path}}, verify it, review it, locally commit completed work. Use when the user invokes implement-phase or asks to continue, start, or complete a {{project_name}} implementation phase.
---

# Implement Phase

Use `.agents/commands/implement-phase.md` as the canonical workflow. Read it before selecting or implementing a phase, then follow it exactly, including its local commit policy.

Keep documentation read-only unless the current user request explicitly asks for documentation edits. If implementation exposes plan or supporting-doc drift, collect every required update and surface it in the final `Documentation Required` section instead of editing docs. Do not ask the user to update completion notes or phase status icons; completion marking is user-owned.
